/**
 * apps/server — placeholder bootstrap (E3+).
 * Real Fastify app (routes → services → repositories) lands in epics E3/E4/E5.
 */
export function placeholder(): string {
  return '@po/server: not implemented yet';
}
