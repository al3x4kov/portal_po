/**
 * apps/web — placeholder entry (E6).
 * Real React + Vite SPA (pages, components, api client) lands in epic E6.
 */
export function placeholder(): string {
  return '@po/web: not implemented yet';
}
