# Границы системы и контекст (Volere) — «Управление требованиями для PO»

> Версия 1.1 · 2026-07-07 · Дополняет project.md (§1.2 акторы, §3 FR). SA-1.
> v1.1 — добавлен внешний актор **«Внешний LLM-провайдер (AI Hub)»** и бизнес-события
> AI-подсистемы (события #25–#29); синхронизация границ с фактической реализацией по итогам
> совета (SA-1). Архитектура интеграции — [`../architecture/ADR-007-ai-hub-integration.md`](../architecture/ADR-007-ai-hub-integration.md).

Документ фиксирует **границы работы (work scope)** и **контекст системы** по методологии
Volere (Джеймс Робертсон): кто взаимодействует с системой (акторы), какие бизнес-события
пересекают её границу и что система выдаёт в ответ. Разграничение work scope vs product
scope — в §4.

Связанные документы: [`project.md`](project.md) (ТЗ, роли §1.2, FR §3, вне скоупа §1.4),
[`../architecture/api.md`](../architecture/api.md) (REST `?format=openspec`, MCP-tools),
[`../architecture/ADR-001-openspec-storage.md`](../architecture/ADR-001-openspec-storage.md)
(формат хранения OpenSpec/slug — источник истины).

---

## 1. Акторы

- **Product Owner (человек, первичный актор)** — локальный пользователь приложения. Через
  UI создаёт/редактирует ФТ и НФТ, строит дерево связей, импортирует и экспортирует
  архивы. Единственная роль-человек в MVP (аутентификация/права вне скоупа, project.md §1.4).

- **ИИ-агент через MCP (программный, stdio)** — клиент MCP-сервера `apps/mcp` (`@po/mcp`).
  Читает и (опционально) изменяет требования через MCP-tools поверх сервисов `@po/server`
  и общего `PROJECTS_ROOT`. Не человек, работает без UI. Доменные ошибки получает разборно
  (`isError: true`, текст `[CODE] message`).

- **ИИ-агент / скрипт через REST (программный, HTTP)** — клиент REST-контура `apps/server`,
  запрашивает машиночитаемый OpenSpec через `GET /api/projects/:id/requirements?format=openspec`
  (детерминированный, байт-в-байт стабильный вывод). Не человек, без UI.

- **Файловая система / внешний редактор (внешний источник)** — редактор или git-операция
  вне приложения, изменяющая `.md`-файлы требований напрямую в `Projects/`. Изменения
  приложение не контролирует; при чтении обязано валидировать и не падать.

- **Архив импорта (внешний источник)** — файл `.zip`/`.tar.gz` со структурой OpenSpec-проекта,
  передаваемый PO при импорте. Внешний по отношению к системе носитель данных.

- **Внешний LLM-провайдер (AI Hub) (внешняя система, HTTP)** `[ДОБАВЛЕНО v1.1]` — внешний
  OpenAI-совместимый сервис, к которому система обращается **исходящими** запросами для
  AI-функций (чат-помощник, генерация описания требования, AI-импорт требований из документации).
  Наружу уходят: сообщения чата, контекст требования/проекта и **содержимое загруженной
  документации**. Конфигурируется PO (`baseURL`, `apiKey`, модель) на странице AI. Единственная
  исходящая сетевая зависимость; ядро продукта работает без неё (деградация). Границы, что
  именно уходит наружу, хранение ключа и **принятый риск** — в
  [`../architecture/ADR-007-ai-hub-integration.md`](../architecture/ADR-007-ai-hub-integration.md).

Первичный актор-человек — **Product Owner**; остальные акторы — программные клиенты или
внешние источники данных. **Внешний LLM-провайдер** — единственная внешняя система, к которой
система обращается сама (исходящая интеграция).

---

## 2. Контекстная диаграмма

```
                      Product Owner (человек)
                              │
              UI-действия ▼   │   ▲ экраны, дерево,
              (CRUD, связи,   │   │ уведомления, скачивание
               импорт/экспорт)│   │ архива/xlsx
                              ▼   │
   ИИ-агент (MCP, stdio)  ┌───────┴────────────────────────┐   Архив импорта (.zip/.tar.gz)
   ── вызовы tools ──────▶│                                │◀── файл архива ── (вход, FR-3)
   ◀── requirement/       │   СИСТЕМА                       │
       broken/deleted ────│   «Управление требованиями PO»  │── архив .zip/.tar.gz ──▶
                          │                                │   / файл .xlsx (выход, FR-10)
   ИИ-агент/скрипт (REST) │   apps/web · apps/server ·      │
   ── GET ?format=openspec│   apps/mcp · packages/core      │
      ───────────────────▶│   хранилище: Projects/ (OpenSpec)│
   ◀── OpenSpec text /     │                                │
       JSON+broken ───────└───────┬────────────────────────┘
                                  │   ▲
             запись .md (atomic)  ▼   │  чтение .md
             экспорт/импорт           │  (валидация, пометка broken)
                          ┌───────────┴───────────┐
                          │  Файловая система /     │
                          │  внешний редактор       │
                          │  (ручная правка .md)    │
                          └────────────────────────┘
```

Легенда: стрелки `──▶` — вход в систему, `◀──` — выход из системы. Хранилище `Projects/`
показано внутри границы системы (источник истины), но правится и снаружи — внешним
редактором (нижний актор).

---

## 3. Бизнес-события (вход/выход)

Каждое событие пересекает границу системы. Столбец «FR / MCP-tool» связывает событие с
функциональным требованием project.md §3 и/или именем MCP-tool (api.md).

| # | Событие | Актор | Вход | Реакция системы / Выход | FR / MCP-tool |
|---|---------|-------|------|--------------------------|----------------|
| 1 | Открыт стартовый экран | Product Owner | запуск приложения | Экран с тремя действиями: «Новый проект» / «Импорт» / «Открыть существующий» | FR-1 |
| 2 | Создан новый проект | Product Owner | имя проекта | Создан (при отсутствии — воссоздан) `Projects/`; создан каталог проекта; переход на основной экран + уведомление с Main Path; дубль имени → ошибка | FR-2 |
| 3 | Импортирован архив проекта | Product Owner, Архив импорта | имя проекта + файл `.zip`/`.tar.gz` | Распаковка во временную папку, валидация структуры и схемы, атомарный `rename` в `Projects/<имя>`; при повреждении — понятная ошибка без полусобранного каталога | FR-3 |
| 4 | Открыт существующий проект | Product Owner | выбор каталога из списка | Скан `Projects/`, список каталогов + «Назад»; выбор открывает основной экран проекта | FR-4 |
| 5 | Показан основной экран | Product Owner | открытие проекта | Top: Main Path; Middle: дерево/таблица ФТ и НФТ; Bottom: «Добавить Функцию/НФТ», «Экспорт в архив» | FR-5 |
| 6 | Создано требование | Product Owner | поля формы (name, criticality, description, implemented, quarter/year) | Валидация (уникальность имени, проверяемость, условные поля); генерация slug; атомарная запись `.md`; строка в дереве | FR-6 |
| 7 | Отредактировано требование | Product Owner | изменённые поля + подтверждение сохранения | Перепроверка правил; slug/type неизменны; атомарное обновление `.md` | FR-6.5 |
| 8 | Проверено имя на уникальность | Product Owner | вводимое name (real-time) | Дебаунс-ответ «свободно/занято»; не даёт сохранить дубль | FR-6.6 |
| 9 | Раскрыт/свёрнут узел дерева, открыто описание | Product Owner | клик по узлу/описанию | Раскрытие вложенности с уровнем; длинное описание в боковой панели (drawer) | FR-7 |
| 10 | Создана связь между требованиями | Product Owner | тип связи + целевое требование | Проверки целостности (цикл, один родитель, self-link, согласованность типов); запись взаимно-обратной пары в оба `.md`; при нарушении — ошибка с путём | FR-8 |
| 11 | Удалено требование | Product Owner | подтверждение «точно удалить?» | Удаление файла + чистка всех обратных ссылок; запрет при наличии потомков (по умолчанию) | FR-9 |
| 12 | Экспортирован проект в архив | Product Owner | выбор формата `.zip`/`.tar.gz` | Сборка самодостаточного архива (`.md` + манифест) во временный файл → поток на скачивание (round-trip) | FR-10, `export_project` |
| 13 | Экспортирован проект в Excel | Product Owner | запрос выгрузки `.xlsx` | Файл `.xlsx` (листы `Requirements`, `Links`); только выгрузка, импорт xlsx не поддерживается | FR-10 (§8.3) |
| 14 | Запрошен OpenSpec-текст требований | ИИ-агент/скрипт (REST) | `GET /api/projects/:id/requirements?format=openspec` | `text/markdown`: заголовок `# OpenSpec: <id>` + секции `## functions`/`## nfr` с `### Requirement:`; детерминированный байт-в-байт; неверный format → `400`, нет проекта → `404` | REST `?format=openspec` |
| 15 | Запрошен список требований (JSON) | ИИ-агент/скрипт (REST), ИИ-агент (MCP) | `GET .../requirements` (json) / `list_requirements` | `{ requirements: [], broken: [] }`; пустой проект → пустые массивы, не ошибка | `list_requirements` |
| 16 | Запрошен список проектов | ИИ-агент (MCP) | — | `{ projects: ProjectSummary[] }` | `list_projects` |
| 17 | Запрошен один проект | ИИ-агент (MCP) | `projectId` | `{ project: ProjectSummary }`; нет проекта → `[NOT_FOUND]` | `get_project` |
| 18 | Запрошено одно требование | ИИ-агент (MCP) | `projectId, slug` | `{ requirement }`; нет — `[NOT_FOUND]` | `get_requirement` |
| 19 | Создано требование (агент) | ИИ-агент (MCP) | `projectId, type, name, criticality, description?, implemented, targetQuarter?, targetYear?` | `{ requirement }` (итоговое состояние); доменные ошибки (`UNIQUENESS`, `VALIDATION`) разборно | `create_requirement` |
| 20 | Обновлено требование (агент) | ИИ-агент (MCP) | `projectId, slug, name, criticality, …` | `{ requirement }`; slug/type неизменны | `update_requirement` |
| 21 | Связаны требования (агент) | ИИ-агент (MCP) | `projectId, sourceSlug, type, targetSlug` | `{ requirement }` (source после связи); проверки `CYCLE`/`SELF_LINK`/`MULTIPLE_PARENT` разборно | `link_requirements` |
| 22 | Удалено требование (агент) | ИИ-агент (MCP) | `projectId, slug` | `{ deleted: true, slug }`; чистка обратных ссылок; `HAS_CHILDREN` при потомках | `delete_requirement` |
| 23 | Экспортирован проект (агент) | ИИ-агент (MCP) | `projectId, format: zip\|targz` | `{ path, filename, contentType, bytes }` (архив во временном файле) | `export_project` |
| 24 | Внешний редактор изменил `.md` | Файловая система / внешний редактор | правка/повреждение файла требования вне приложения | При следующем чтении система **валидирует** файл, помечает требование как **broken** (в `broken[]` / отчёте целостности), **не падает**; корректные требования доступны | FR-3.3, §5 (краевые случаи) |
| 25 | Сохранена/удалена AI-конфигурация | Product Owner ↔ Внешний LLM-провайдер | `baseURL`, `apiKey`, модель, пресеты (`PUT /api/ai/config`); запрос списка моделей (`GET /api/ai/models`) | Конфиг сохранён в `.ai-config.json` (ключ plaintext, вне экспорта); `GET` возвращает `hasApiKey`, но не ключ; список моделей запрашивается у провайдера | FR-17 |
| 26 | Отправлено сообщение в AI-чат | Product Owner → Внешний LLM-провайдер | текст + история (≤20) (`POST /api/ai/chat`) | Системный промпт + история уходят провайдеру; ответ ассистента возвращается в чат-виджет; при недоступности — `{code,message}` без падения | FR-18 |
| 27 | Сгенерировано описание требования | Product Owner → Внешний LLM-провайдер | контекст требования/проекта + подсказка (`POST /api/ai/generate-description`) | Контекст уходит провайдеру; сгенерированный текст предлагается в карточке (PO принимает/правит) | FR-19 |
| 28 | Запущен AI-импорт из документации | Product Owner, Архив импорта → Внешний LLM-провайдер | архив документации (`POST /api/projects/:id/ai-import`) | Содержимое документов чанками уходит провайдеру; извлекаются ФТ/НФТ и связи; возвращается `jobId` (async) | FR-20 |
| 29 | Опрошен/отменён статус AI-импорта | Product Owner | `jobId` (`GET /api/ai-import/:jobId`; отмена) | Прогресс/лог/результат джобы (in-memory); отмена останавливает джобу; истёкший/после рестарта job → `NOT_FOUND` | FR-20 |

Покрытие: события 1–13 покрывают FR-1…FR-10 (стартовый экран, новый проект, импорт,
открытие, основной экран, CRUD, дерево/представление, связывание, удаление, экспорт
zip/tar.gz/xlsx); событие 14 — REST `?format=openspec`; события 15–23 — все MCP-tools
(`list_projects`, `get_project`, `list_requirements`, `get_requirement`,
`create_requirement`, `update_requirement`, `link_requirements`, `delete_requirement`,
`export_project`); событие 24 — внешняя правка `.md`; **события 25–29 — AI-подсистема
(конфиг, чат, генерация описания, старт и опрос AI-импорта), актор «Внешний LLM-провайдер»,
FR-17…FR-20, ADR-007** `[ДОБАВЛЕНО v1.1]`.

## Business Use Cases (BUC) `[ДОБАВЛЕНО v1.1 — SA-6]`

BUC описывают, **что бизнес/PO хочет достичь безотносительно продукта** (цель, а не UI-шаги), и
связывают цепочку «бизнес-событие → BUC → Product Use Case (PUC, `project.md` §10)». Это отделяет
потребности от реализации и даёт gap-анализ «нужды vs возможности».

| BUC | Цель PO (бизнес) | Триггер-события | Реализующий PUC/FR | Покрытие |
|---|---|---|---|---|
| **BUC-1 Завести управляемый набор требований** | иметь единый структурированный источник требований продукта | #1–#6 | PUC (создание проекта/требований), FR-1…FR-6 | ✅ |
| **BUC-2 Организовать требования в целостную структуру** | видеть иерархию и зависимости, без противоречий | #7–#9 | PUC (дерево/связи), FR-7/FR-8, 2.4 | ✅ |
| **BUC-3 Обменяться требованиями (архив/выгрузка)** | передать/забрать набор без потерь, поделиться в Excel | #10–#12 | PUC-3 (импорт/экспорт), FR-3/FR-10 | ✅ |
| **BUC-4 Отдать требования ИИ-агенту как источник истины** | чтобы агент разрабатывал по актуальным требованиям | #14–#23 | PUC (OpenSpec/MCP), FR-10.4, MCP-tools | ✅ (главная гипотеза, метрики PO-4) |
| **BUC-5 Быстро превратить документацию в требования** | сэкономить время на первичном вводе из сырого документа | #28–#29 | PUC AI-импорта, FR-20 | ✅ (вторичная гипотеза, риск-план PO-5) |
| **BUC-6 Получить помощь в формулировании** | ускорить написание описаний/уточнений | #26–#27 | FR-18/FR-19 (чат, генерация) | ✅ |
| **BUC-7 Сохранять доверие к данным при повреждении** | не терять работоспособность при битых файлах/архивах | #24, импорт | §5 (broken-панель SA-4, reject SA-3), FR-3.3/3.4 | ✅ |

**Gap-анализ:** каждый BUC имеет реализующий PUC/FR (нет непокрытых бизнес-нужд в текущем скоупе).
Незакрытое ранее (история/undo — §7.4) переведено в «вне MVP» (git вне приложения) — не образует
незакрытого BUC. Новые бизнес-нужды заносить сюда и проверять покрытие PUC.

---

## 4. Вне области (Out of scope)

Volere различает **границу работы (work scope)** — что система вообще обрабатывает как
события — и **границу продукта (product scope)** — какие из этих событий автоматизирует
поставляемый MVP. Ниже перечислено то, что **вне области**: либо не является событием
системы вовсе (за пределами work scope), либо сознательно исключено из MVP (за пределами
product scope).

**За пределами work scope (система этим не занимается вообще):**
- **Аутентификация и разграничение прав** — приложение локальное, single-user; нет
  события «вход пользователя», нет ролей/ACL (project.md §1.4).
- **Многопользовательский режим и совместное редактирование** — нет событий от нескольких
  одновременных пользователей; конкурентная правка файла = «последний записал побеждает» + лог.
- **Облачное хранилище и синхронизация между устройствами** — источник истины только
  локальная ФС `Projects/`; события синхронизации отсутствуют. `[УТОЧНЕНО v1.1]` Это **не**
  исключает исходящую **AI-интеграцию**: обращение к внешнему LLM-провайдеру (AI Hub) — это
  запрос-ответ для AI-функций, а **не** хранение/синхронизация данных проекта вовне. Ядро
  (источник истины, CRUD, экспорт) остаётся локальным и работает без сети; AI-функции —
  опциональная деградируемая надстройка (события #25–#29, FR-17…FR-20, ADR-007).
- **Версионирование / история изменений требований** — кроме того, что даёт **git вне
  приложения**; внутри приложения нет undo/истории/diff как событий.
- **Редактирование сырого Markdown как текста** — приложение управляет структурой (поля,
  связи), а не свободным текстом файла; «сохранить произвольный MD» не является событием
  системы (правка сырого файла проходит только через внешнего актора, событие #24).

**За пределами product scope (событие возможно, но в MVP не автоматизировано):**
- **Импорт `.xlsx`** — только выгрузка (событие #13); round-trip остаётся **archive-only**
  (импорт лишь `.zip`/`.tar.gz`). Обратный разбор Excel в MVP не реализуется.
- **Авторинг сценариев (`#### Scenario:`) в UI** — сценарии **read-only из импорта**;
  создание/редактирование сценариев через UI **вне MVP** (project.md §2.1, §2.6, SA-10).
- **Миграция схемы при `schemaVersion` ≠ текущей** — при несовпадении версии схемы в MVP
  система выдаёт **ошибку** (не мигрирует автоматически); автоконвертация форматов вне MVP.
