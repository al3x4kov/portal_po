# E11 — UI-доработки (FE, TDD) — по макетам Фазы B (docs/design/)

Основание: `docs/architecture/PLAN-dorabotka.md` (A6#4 — единый слой видимости строк).
Все фичи, влияющие на видимость строк дерева, реализуются через **один** слой
`computeVisibleRows({ tree, search, collapsed, criticalityFilter })` — не разрозненными
флагами. Правило: если строка-совпадение видна, её **предки тоже видны**.

## T-1101 · Переключатель раскрыть/свернуть (исх.3, B1)
- Кнопка «Раскрыть все» (по умолчанию) / «Скрыть зависимости» (сворачивание связанных).
- **Приёмка:** компонентный тест; S24, S25.

## T-1102 · Новая раскладка таблицы (исх.4, B2)
- Убрать лишнее пустое пространство на широком мониторе (макет B2). Сохранить/обновить
  `data-testid`, синхронизировать с QA.
- **Приёмка:** визуальная согласованность с макетом; тесты не падают по селекторам.

## T-1103 · Поиск по названию с показом предков (исх.5, B3)
- Текстовый поиск по колонке названия; совпадение на потомке показывает и предков.
- **Приёмка:** S26, S29 (empty-состояние).

## T-1104 · Показ длинного описания (исх.6, B4) — закрывает FR-7.4
- Раскрытие/поповер/боковая панель по клику (по макету B4).
- **Приёмка:** S30 (без обрезки, доступно с клавиатуры).

## T-1105 · Мультиселект-фильтр критичности (исх.7, B5)
- Фильтр на колонке «Критичность», через общий слой видимости (показывать предков).
- **Приёмка:** S27, S28 (совместно с поиском/сворачиванием — без «сирот»).

## T-1106 · Обновлённые модалки ФТ/НФТ (исх.8, B6)
- Пересмотренные модалки создания/редактирования по макету B6 (та же zod-схема из core).
- **Приёмка:** компонентные тесты модалок зелёные; уникальность имени (FR-6.6) работает.

## DoD
- Vitest+RTL зелёные; `typecheck`/`lint` OK; `data-testid` согласованы с E2E (E12).
