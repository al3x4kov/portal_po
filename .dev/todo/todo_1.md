# todo_1 — доработка project_po (структурированный бэклог)

> Преобразовано из исходного списка задач. Решения по уточняющим вопросам (2026-07-01):
> 1. **OpenSpec** — берём **реальный формат/инструмент** OpenSpec (Fission-AI), не «в стиле».
> 2. **ID требований** — решение за архитектором (взвесить целостность связей vs читаемость).
> 3. **Объём** — полная автономная сборка: план → дизайн → реализация (TDD) → E2E → README.
> 4. **Миграция** — данных на диске нет; формат хранения можно менять без обратной совместимости.

Владельцы (роли): **Арх** — архитектор (планирование), **Диз** — дизайнер (макеты),
**BE** — backend-node-senior, **FE** — frontend-ts-senior, **QA** — playwright-qa-senior.
Разработка ведётся **от тестов** (TDD): сначала тесты по критериям приёмки, затем код (п.14).

---

## Фаза A — Планирование (Арх) → артефакты в `docs/architecture/`, задачи в `docs/tasks/`

- [ ] **A1 (исх. 1) OpenSpec-хранилище проекта.** Пересмотреть работу с `.md` в сторону
  реального формата OpenSpec: `openspec/specs/<domain>/spec.md` с
  `### Requirement:` / `#### Scenario:`, дельты в `changes/`. Отдельный файл на каждое
  ФТ/НФТ внутри Project. Решить судьбу `id` (нужен ли для связей; если нет — убрать) →
  ADR. Спланировать задачи миграции хранилища. **Зависит от:** —
- [ ] **A2 (исх. 2) Экспорт в Excel.** Спланировать экспорт `.xlsx` помимо архивов
  (лист требований + лист связей; контракт эндпоинта). **Зависит от:** A1 (модель данных).
- [ ] **A3 (исх. 9) AI-ready API.** Продумать REST-контур для агентов + MCP-сервер
  (tools: list/get/create/update/link/export). Сформулировать задачи на реализацию.
  **Зависит от:** A1.
- [ ] **A4 (исх. 10) Матрица ситуаций.** Список ситуаций, где система ведёт себя
  корректно/некорректно → задачи на автотесты (unit + E2E). **Зависит от:** A1.
- [ ] **A5 (исх. 11) Покрытие unit-тестами.** Оценить текущее покрытие, поставить задачи
  BE, если не устраивает. *Факт на 2026-07-01: core 98.5%/94.6% branch; `apps/server` в
  метрике покрытия не участвует (gate только по core).* **Зависит от:** —
- [ ] **A6 (исх. 12) Ревизия коллизий.** Пройтись по всему списку, найти конфликты
  постановок, поправить. *Уже найдено: удаление `id` (исх.1) конфликтует со связями по
  `targetId` — см. A1/ADR.* **Зависит от:** A1–A5.

## Фаза B — Дизайн (Диз) → макеты HTML+Tailwind в `docs/design/`, обновить `docs/design/DESIGN.md`

- [ ] **B1 (исх. 3) Переключатель отображения.** Кнопка «Раскрыть все» (по умолчанию) /
  «Скрыть зависимости» (сворачивание связанных функций). Макеты. **Зависит от:** —
- [ ] **B2 (исх. 4) Использование ширины экрана.** Пусто на широком мониторе — пересмотреть
  раскладку таблицы ФТ/НФТ по мировой практике, новые макеты. **Зависит от:** —
- [ ] **B3 (исх. 5) Поиск по колонке названия.** Текстовый поиск; при совпадении на потомке
  показывать и его предков. Макеты. **Зависит от:** B2.
- [ ] **B4 (исх. 6) Колонка «Описание».** Узкая; современные способы показать длинный текст
  по клику (поповер/раскрытие/боковая панель). Макеты. Закрывает FR-7.4. **Зависит от:** B2.
- [ ] **B5 (исх. 7) Фильтр «Критичность».** Мультиселект-фильтр на колонке. Макеты.
  **Зависит от:** B2.
- [ ] **B6 (исх. 8) Модалки ФТ/НФТ.** Пересмотреть модальные окна создания/редактирования.
  Макеты. **Зависит от:** —
- [ ] **B7.** Арх оценивает изменения B1–B6 и планирует задачи на доработку (в `docs/tasks/`).

## Фаза C — Реализация backend (BE, TDD)

- [ ] **C1** OpenSpec-хранилище (сериализация/парсинг spec.md, репозитории, целостность).
- [ ] **C2** Экспорт `.xlsx` (эндпоинт + сервис).
- [ ] **C3** AI-ready REST + MCP-сервер.
- [ ] **C4** Доведение покрытия по итогам A5 (интеграционные тесты сервера в метрику).

## Фаза D — Реализация frontend (FE, TDD)

- [ ] **D1** Переключатель раскрыть/свернуть (B1).
- [ ] **D2** Новая раскладка таблицы (B2).
- [ ] **D3** Поиск с показом предков (B3).
- [ ] **D4** Показ длинного описания (B4).
- [ ] **D5** Мультиселект-фильтр критичности (B5).
- [ ] **D6** Обновлённые модалки (B6).
- [ ] **D7** Кнопка экспорта в Excel (A2/C2).

## Фаза E — Качество и запуск

- [ ] **E1 (исх. 13)** Запустить выполнение задач для агентов (оркестрация фаз C/D).
- [ ] **E2 (исх. 10/QA)** E2E-автотесты по матрице ситуаций (A4).
- [ ] **E3 (исх. 15)** `README.md` в корне `project_po` — для человека «с нуля»: что это,
  как локально запустить, как пользоваться.

## Сквозные принципы

- **(исх. 14)** Всё строим от тестов (TDD): красный → зелёный → рефактор.
- Зелёный CI на каждом шаге: `format:check` → `lint` → `typecheck` → unit → e2e.
- После изменений кода — `graphify update .`, если есть `graphify-out/`.
