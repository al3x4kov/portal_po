# T6 — Dashboard, GraphView, DescPanel

Читай сначала: .dev/todo/task17_common.md, макеты new_design/screens/{dashboard,graph-view,desc-panel}.html.
Код: apps/web/src/pages/Dashboard.tsx, components/GraphView/GraphView.tsx, components/DescPanel.tsx (+тесты).

## Dashboard (dashboard.html)
- Карточка «Качество описаний» видна ВСЕГДА (при нуле проблем — зелёное состояние «всё в порядке»);
- Списки проблем: лимит 7 + «Показать все (N)»;
- Подписи осей/меток графиков ≥10px (по макету), остальной текст ≥11px.

## GraphView (graph-view.html)
- Битые узлы (ошибки целостности): красный пунктирный контур + значок ⚠ + тултип с текстом ошибки;
- Оверлей на время пересчёта лейаута; легенда типов/состояний по макету.

## DescPanel (desc-panel.html)
- Markdown-рендер описания (безопасно, без сырого HTML — см. common);
- Свёрнутый режим line-clamp-2 с разворачиванием;
- Связи — чипами (тип по-русски + имя, клик = переход к требованию, если так в макете);
- Безопасное удаление: confirm по уровням из T4.

## Приёмка
- Средний род во всех текстах; данные/расчёты дашборда и графа не менялись — только представление; компонентные тесты обновлены.
