# todo_16 — фиксы наблюдаемости AI-импорта (по read-only аудиту)

Требование пользователя: пользователь ДОЛЖЕН постоянно видеть, что происходит в данный момент;
все тексты ошибок — понятные и с причиной; позитив и негатив протестировать моками; баги чинить.

Цепочка: routes/aiImport.ts → AiImportService.run() → AiImportJobs (view) → useAiImportJob
(поллинг 800мс) → AiImportModal. Прогресс/лог формирует бэкенд, фронт рендерит.

## Backend (владелец: backend-агент; файлы AiImportService.ts, AiImportJobs.ts при необходимости, тесты)

Ф1. Распаковка «слепая»: AiImportService.ts:616-620 — одна строка до unpackDocsArchive(), затем
    тишина при 0%. Добавить: лог с размером архива перед распаковкой («Распаковка архива (X МБ)…»),
    после старта распаковки progress=2; после — существующая строка «Найдено файлов: N».

Ф2. Чтение/нарезка файлов в начале analyze без логов (652-657, стадия analyze уже показана, а
    прогресс 5): добавить лог «Чтение и подготовка файлов документации…» перед циклом.

Ф3. ГЛАВНОЕ: нет лог-строки ПЕРЕД каждым AI-вызовом (самые долгие операции выглядят зависанием).
    Добавить перед вызовом модели:
    - analyze, цикл ~673-756: «Файл <file> (фрагмент i/N): запрос к модели…»
    - structure, ~796: «Структура: батч i/N — запрос к модели…»
    - relate, ~465: «Связи ФТ↔НФТ: запрос к модели…»

Ф4. Внутренняя ошибка (belt-and-braces catch в start(), 256-259): сейчас hint про формат архива и
    сырой JS-текст. Сделать: message «Внутренняя ошибка автоматизации.», hint
    «Повторите анализ; если ошибка повторяется — обратитесь к администратору.» (константа
    AI_IMPORT_HINT_INTERNAL); сырой текст — только в лог (sanitized).

Ф5. Relate при stage='populate' и bar 99%: минимально — лог-строка перед AI-вызовом (Ф3) и
    итоговая строка результата уже есть; прогресс во время relate держать на 99 — ок, но
    убедиться, что relate.status='running' выставляется ДО AI-вызова (фронт покажет «выполняется…»).

Тесты (ai-import-service.test.ts + resilience): наличие новых pre-call строк в логе (позитив);
Ф4 — текст message/hint при искусственном крэше (негатив); relate.status='running' виден до
завершения вызова; существующие тесты не ломать. Прогресс-значения по стадиям — добавить
базовые ассерты, если легко.

## Frontend (владелец: frontend-агент; файлы AiImportModal.tsx, тесты)

Ф6. Подпись «Этап:» во время relate: когда job.relate?.status==='running' — показывать
    «Проставление связей ФТ↔НФТ» вместо stageLabel (сейчас «Наполнение проекта»).
    ВНИМАНИЕ: тест AiImportModal.test.tsx:449 закрепляет старое поведение — обновить его.

Ф7. Сводка при отмене: при phase==='cancelled' и наличии job.result показать панель
    (data-testid="ai-import-cancelled-summary"): «Остановлено. Успели создать: N ФТ, M НФТ,
    связей K…» — по полям result (как в success-панели).

Ф8. Индикатор предупреждений: рядом с логом бейдж/строка «Предупреждений: N» (count warn/error
    строк лога, data-testid="ai-import-warn-count"), видим и по завершении; 0 — не показывать.

Ф9. jobLost-текст (строка ~508): заменить на «Статус задания недоступен (возможно, сервер был
    перезапущен или задание устарело). Запустите анализ заново.»

Ф10. Клиентская проверка размера файла при выборе (>50 МБ): сразу показать ошибку
    («Файл больше 50 МБ — уменьшите архив документации.») и не отправлять. Лимит взять из
    существующей константы, если она экспортируется из @po/core; иначе локально 50*1024*1024
    с комментарием-ссылкой на серверный лимит.

Тесты (AiImportModal.test.tsx): Ф6 (обновить :449 + новый), Ф7 позитив (result есть) и негатив
(result нет — панели нет), Ф8 (0 → нет бейджа; N warn → «Предупреждений: N»), Ф9 текст,
Ф10 (большой файл → ошибка, старт не вызван; нормальный — уходит). Моками покрыть позитивные
И негативные сценарии. Существующие тесты (A3, B2, relate-тексты) не ломать, data-testid
ai-import-* и ai-models-* сохранить.

## Общие правила
- Сначала graphify query, потом файлы. Не коммитить, graphify update не запускать, e2e не гонять
  (перегоняет оркестратор в финале). Контракт @po/core менять только если строго необходимо
  (Ф4-хинт — константа может жить рядом с существующими хинтами на сервере).
